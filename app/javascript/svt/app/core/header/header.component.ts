import { Component } from '@angular/core';
import templateString from './header.component.html';

@Component({
  selector: 'app-header',
  template: templateString,
})
export class HeaderComponent {
  
}